import React, { FC } from 'react';
import classes from '../styles/MyHeader.module.css';

const MyHeader: FC = () => {
  return (
    <div className={classes.myHeader}>Header</div>
  );
};

export default MyHeader;